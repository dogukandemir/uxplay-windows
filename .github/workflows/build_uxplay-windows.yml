name: build uxplay-windows
on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      tmate_debug_enabled:
        description: 'Run the build with tmate debugging enabled (https://github.com/marketplace/actions/debugging-with-tmate)'
        default: false
        required: false
        type: boolean

jobs:
  build:
    name: build uxplay
    runs-on: windows-latest    
    permissions:
      contents: write
    
    steps:
      - name: clone the repo
        uses: actions/checkout@v4
          
      - name: download+install mDNSResponder libraries
        shell: pwsh
        run: |
          git clone -b rel/mDNSResponder-214 https://github.com/apple-oss-distributions/mDNSResponder.git --depth 1 mdns214

          # Build dnssd.dll + dnssd.lib (client DLL used by UxPlay and bundled in the portable ZIP)
          Copy-Item dnssd.vcxproj mdns214\mDNSWindows\DLL\dnssd.vcxproj
          & 'C:\Program Files\Microsoft Visual Studio\2022\Enterprise\MSBuild\Current\Bin\MSBuild.exe' `
              mdns214\mDNSWindows\DLL\dnssd.vcxproj /p:Configuration=Release /p:Platform=x64

          # Install Bonjour SDK headers + import lib so cmake can find them
          New-Item -ItemType Directory -Force "C:\Program Files\Bonjour SDK\Include" | Out-Null
          New-Item -ItemType Directory -Force "C:\Program Files\Bonjour SDK\Lib\x64" | Out-Null
          Copy-Item mdns214\mDNSShared\dns_sd.h  "C:\Program Files\Bonjour SDK\Include\dns_sd.h"
          Copy-Item mdns214\mDNSWindows\DLL\x64\Release\dnssd.lib "C:\Program Files\Bonjour SDK\Lib\x64\dnssd.lib"

          # Build mDNSResponder.exe (the mDNS service daemon bundled for portable use)
          Copy-Item mDNSResponder.vcxproj mdns214\mDNSWindows\SystemService\mDNSResponder.vcxproj
          & 'C:\Program Files\Microsoft Visual Studio\2022\Enterprise\MSBuild\Current\Bin\MSBuild.exe' `
              mdns214\mDNSWindows\SystemService\mDNSResponder.vcxproj /p:Configuration=Release /p:Platform=x64

      - name: download+install msys2 and related build dependencies
        uses: msys2/setup-msys2@v2
        with:
          update: true
          install: >-
            git
            zip
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-libplist
            mingw-w64-x86_64-json-glib
            mingw-w64-x86_64-gstreamer
            mingw-w64-x86_64-gst-plugins-base
            mingw-w64-x86_64-gst-libav
            mingw-w64-x86_64-gst-plugins-good
            mingw-w64-x86_64-gst-plugins-bad

      - name: compiling uxplay (finally)
        shell: msys2 {0}
        run: |
          git clone https://github.com/FDH2/UxPlay && cd UxPlay
          mkdir build && cd build
          cmake ..
          ninja
          
          mkdir artifact && cd artifact
          mkdir lib
          cp -r /mingw64/lib/gstreamer-1.0 lib/gstreamer-1.0
          cp -r /mingw64/bin bin
          cp ../uxplay.exe bin

        # remove unnecessary DLLs so we get a smaller installer
      - name: Prune compilation artifact directory
        run: |
          $artifactDir = "D:\a\uxplay-windows\uxplay-windows\UxPlay\build\artifact"
          $keepListFile = "D:\a\uxplay-windows\uxplay-windows\dll-libs-list.txt"
          
          # Read patterns from the keep list (can include wildcards)
          $keepPatterns = @()
          Get-Content $keepListFile | ForEach-Object {
              $pattern = $_.Trim()
              if ($pattern) {
                  $keepPatterns += $pattern
              }
          }
      
          Write-Host "Total keep patterns: $($keepPatterns.Count)"
      
          # Get all files currently existing in the target directory and its subdirectories
          $allFilesInTarget = Get-ChildItem -Path $artifactDir -Recurse -File
      
          Write-Host "Total files found in target directory before pruning: $($allFilesInTarget.Count)"
      
          $deletedCount = 0
          $keptCount = 0
          
          # Iterate through all files and check if they match any keep pattern
          foreach ($currentFile in $allFilesInTarget) {
              $currentFilePath = $currentFile.FullName
              $shouldKeep = $false
              
              # Check if the current file matches any of the keep patterns
              foreach ($pattern in $keepPatterns) {
                  if ($currentFilePath -like $pattern) {
                      $shouldKeep = $true
                      $keptCount++
                      break
                  }
              }
              
              if (-not $shouldKeep) {
                  Remove-Item $currentFilePath -Force
                  $deletedCount++
              }
          }
      
          Write-Host "Cleanup complete! Kept $keptCount files, deleted $deletedCount files." -ForegroundColor Green
          Write-Host "`nContents of $artifactDir after pruning:"
          Get-ChildItem -Path $artifactDir -Recurse | Select-Object FullName
        shell: powershell

      # Use the dnssd.dll and mDNSResponder.exe built from source in the step above.
      # No external downloads required - both binaries come from the mDNSResponder-214 clone.
      - name: collect Bonjour mDNSResponder and dnssd.dll
        shell: pwsh
        run: |
          $dnssdDll = "mdns214\mDNSWindows\DLL\x64\Release\dnssd.dll"
          $mdnsExe  = "mdns214\mDNSWindows\SystemService\x64\Release\mDNSResponder.exe"

          if (-not (Test-Path $dnssdDll)) {
            Write-Error "Locally built dnssd.dll not found at '$dnssdDll'. Was the dnssd build step successful?"
            exit 1
          }

          $dnssdFull = (Resolve-Path $dnssdDll).Path
          Write-Host "dnssd.dll : $dnssdFull"
          "DNSSD_DLL=$dnssdFull" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

          if (Test-Path $mdnsExe) {
            $mdnsFull = (Resolve-Path $mdnsExe).Path
            Write-Host "mDNSResponder.exe : $mdnsFull"
            "MDNS_EXE=$mdnsFull" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          } else {
            Write-Warning "mDNSResponder.exe not found - mDNS service will not be bundled in the portable ZIP."
            Write-Warning "Users will need Apple Bonjour installed for mDNS discovery."
          }

      # compiling the python tray icon into an .exe
      - name: install pyinstaller and dependencies
        run: |
          python -m venv ./venv
          .\venv\Scripts\Activate.ps1
          python -m pip install --upgrade pip
          pip install pyinstaller pystray pillow

          pyinstaller --noconsole --onedir --clean --name uxplay-windows --icon=icon.ico tray.py

          # Copy UxPlay binaries and GStreamer libraries
          cp -r D:\a\uxplay-windows\uxplay-windows\UxPlay\build\artifact\* D:\a\uxplay-windows\uxplay-windows\dist\uxplay-windows\_internal

          # Copy the app icon into _internal so tray.py can find it at runtime
          cp D:\a\uxplay-windows\uxplay-windows\icon.ico D:\a\uxplay-windows\uxplay-windows\dist\uxplay-windows\_internal

          # Bundle dnssd.dll for portable use (required - uxplay.exe links against it at runtime).
          cp "$env:DNSSD_DLL"  D:\a\uxplay-windows\uxplay-windows\dist\uxplay-windows\_internal\bin\

          # Bundle mDNSResponder.exe if it was built successfully (optional - graceful degradation
          # if the mDNSResponder build failed; users with Bonjour installed will still get mDNS).
          if ($env:MDNS_EXE -and (Test-Path $env:MDNS_EXE)) {
            cp "$env:MDNS_EXE" D:\a\uxplay-windows\uxplay-windows\dist\uxplay-windows\_internal\bin\
          } else {
            Write-Warning "Skipping mDNSResponder.exe bundle - file not found."
          }

      - name: create portable ZIP
        shell: powershell
        run: |
          $distDir  = "D:\a\uxplay-windows\uxplay-windows\dist\uxplay-windows"
          $zipPath  = "D:\a\uxplay-windows\uxplay-windows\uxplay-windows-portable.zip"

          Write-Host "Packaging portable ZIP from: $distDir"
          Compress-Archive -Path "$distDir\*" -DestinationPath $zipPath -CompressionLevel Optimal

          $sizeMB = [math]::Round((Get-Item $zipPath).Length / 1MB, 1)
          Write-Host "Created portable ZIP ($sizeMB MB): $zipPath"

      - name: upload portable artifact
        uses: actions/upload-artifact@v4
        with:
          name: uxplay-windows-portable
          path: uxplay-windows-portable.zip

      # move this between steps for debugging
      - name: (optional) Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.tmate_debug_enabled }}
